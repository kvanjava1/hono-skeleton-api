import type { Hono } from "hono";
// import { registerTestRoutes } from "./example/test.routes.ts";
// import { registerUserRoutes } from "./example/user.routes.ts";
// import { registerQueueRoutes } from "./example/queue.routes.ts";

export const registerAllRoutes = (app: Hono) => {
  // registerTestRoutes(app);
  // registerUserRoutes(app);
  // registerQueueRoutes(app);
};
